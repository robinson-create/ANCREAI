# Clerk Authentication (leave empty or use a real key from https://dashboard.clerk.com — pk_test_xxx is a placeholder and will disable auth)
VITE_CLERK_PUBLISHABLE_KEY=

# Stripe (optional, for client-side display)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Backend API base URL (leave empty in dev — Vite proxy handles it; set in prod)
# Example: https://api.ancre.app
VITE_API_BASE_URL=

# CopilotKit Runtime URL (proxied in dev via vite; set absolute URL in prod or leave empty to disable)
VITE_COPILOTKIT_RUNTIME_URL=/copilotkit

# Nango public key (for frontend widget, if used)
VITE_NANGO_PUBLIC_KEY=nango-dev-public-key
